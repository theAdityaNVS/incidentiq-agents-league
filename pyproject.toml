[project]
name = "incidentiq"
version = "0.1.0"
description = "Root-cause reasoning agent for production incidents (Microsoft Foundry)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
